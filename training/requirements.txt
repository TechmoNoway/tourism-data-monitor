# Training-specific requirements for PhoBERT Topic Classifier
# Install with: pip install -r training/requirements.txt

# PyTorch with CUDA support (adjust for your CUDA version)
# For CUDA 11.8: (compatible with CUDA 12.x and 13.x)
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Transformers and ML libraries
transformers>=4.30.0
datasets>=2.14.0
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.24.0

# Database (already in main requirements but listed for reference)
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0

# Optional: For better progress bars and logging
tqdm>=4.65.0

# Optional: For model deployment to HuggingFace
huggingface-hub>=0.16.0

# Optional: For visualization
matplotlib>=3.7.0
seaborn>=0.12.0
